#Aside from things that should 'just work' in setting up django for testing,
# this deals with things that somehow seem not to work.

sudo apt-get install libmemcached-dev zlib1g-dev
pip install -r ~/code/django/tests/requirements/py3.txt

